// AlamofireImplementations.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import XCTest
import WWFoundation
import SwaggerClient

class SwaggerModelTest: XCTestCase {
{{#models}}{{#model}}
    func test{{classname}}InitWithDictionary() {
        if let jsonObject = SwaggerMockModel.mock{{classname}}JsonObject() as? Dictionary<String, AnyObject> {
            let expectedObject = {{classname}}(dictionary: jsonObject)
            XCTAssertNotNil(expectedObject, "{{classname}} should have been initialized.")
        } else {
            XCTFail("Mock Response provided an invalid object.")
        }
{{#hasRequired}}

        if let jsonObject = SwaggerMockModel.mock{{classname}}BadJsonObject() as? Dictionary<String, AnyObject> {
            let expectedObject = {{classname}}(dictionary: jsonObject)
            XCTAssertNil(expectedObject, "{{classname}} should not be initialized.")
        } else {
            XCTFail("Mock Response provided an invalid object.")
        }
{{/hasRequired}}
    }
{{/model}}{{/models}}	
}